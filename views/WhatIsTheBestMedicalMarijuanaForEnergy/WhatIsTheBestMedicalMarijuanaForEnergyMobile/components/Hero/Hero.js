import styles from './Hero.module.css';

const Hero = () => {
    return (
        <div className={styles.heroBanner}>
            <h1 className={styles.heroText}>What is the Best Medical Marijuana for Energy?</h1>
        </div>
    );
};

export default Hero;
