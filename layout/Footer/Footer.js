// FooterComponent.js
import React from 'react';
import styles from './Footer.module.css'; // Assuming the CSS module file is named FooterComponent.module.css

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <p>Footer</p>
        </footer>
    );
};

export default Footer;
